import 'dotenv/config';import app from './app.js';import { connectDatabase } from './config/db.js';
const port=process.env.PORT||5000;await connectDatabase();const server=app.listen(port,()=>console.log(`ASMMR API running on port ${port}`));const shutdown=signal=>{console.log(`${signal} received. Closing server.`);server.close(()=>process.exit(0));};process.on('SIGTERM',()=>shutdown('SIGTERM'));process.on('SIGINT',()=>shutdown('SIGINT'));
